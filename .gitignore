# dependencies
node_modules/
.pnp
.pnp.*

# next
.next/
out/
build/
next-env.d.ts

# testing
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# env — never commit (CLAUDE.md "Do NOT")
.env
.env.*
!.env.example

# supabase local
supabase/.branches
supabase/.temp
supabase/.env

# editors / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# misc
*.tsbuildinfo
.vercel
.eslintcache

# Screenshots taken while checking a change in a real browser. Throwaway by nature — a 938 kB PNG
# reached the repo in 82a42ff via `git add -A` before this line existed.
.shots/
